# STEPS: 업다운 숫자 게임 만들기

기준 문서: [PRD.md](PRD.md)
단계마다 `index.html`을 더블클릭해서 확인한 뒤 다음 단계로 넘어간다.

---

## 1단계. 뼈대
화면 요소만 배치한다. 동작은 아직 넣지 않는다.

- [x] `index.html`을 만든다. (`<!DOCTYPE html>`, `lang="ko"`, `charset="UTF-8"`, viewport 메타)
- [x] 비어 있는 `<style>`, `<script>` 자리를 둔다.
- [x] PRD 3.4 순서대로 요소를 배치한다. 각 요소에 id를 붙인다.
  - 제목, 안내 문구
  - 결과 메시지 `#message`
  - 입력창 `#guess-input` (`type="number"`, min 1, max 100) + [확인] `#submit-btn`
  - 짝홀 힌트 `#parity-hint` (처음엔 숨김)
  - `#attempts` (시도 횟수), `#remaining` (남은 기회)
  - [다시 시작] `#restart-btn`
- [x] 입력창과 [확인]은 `<form>`으로 감싼다. Enter 제출을 준비하는 것이다.

**확인:** 더블클릭하면 요소가 모두 보인다. (모양은 신경 쓰지 않는다)

---

## 2단계. 기능
PRD 2장(R1~R8)과 3.1~3.3을 구현한다.

- [x] 상태 변수를 만든다: `answer`, `attempts`, `guesses`(입력한 숫자 목록), `isOver`, 상수 `MAX_TRIES = 7`
- [x] `startGame()`: 새 정답을 정하고 상태를 초기화한다. 화면을 시작 상태로 만든 뒤 입력창에 포커스를 준다.
- [x] 폼 submit 이벤트로 제출을 처리한다. `preventDefault`를 쓰고, 버튼 클릭과 Enter를 같은 경로로 받는다.
- [x] 입력을 검사한다: 빈 칸 / 정수 아님 / 범위 밖 / 중복이면 안내만 하고 횟수는 그대로 둔다.
- [x] 판정: 정답 → 성공, 크다/작다 → 업다운 메시지. 7번 모두 쓰면 → `실패! 정답은 N`
- [x] `render()` 함수 하나에서 메시지, 시도 횟수, 남은 기회, 짝홀 힌트, 잠금을 한 번에 갱신한다.
- [x] 짝홀 힌트: 틀린 횟수가 4 이상이고 진행 중일 때만 보인다.
- [x] 끝나면 입력창과 [확인]을 `disabled` 처리한다.
- [x] [다시 시작] → `startGame()`
- [x] 제출 후 입력창을 비우고 포커스를 준다.

**확인:** 콘솔에 `answer`를 출력해 두고 성공, 실패, 7번째 성공, 중복/오류 입력을 모두 해 본다. 확인이 끝나면 출력을 지운다.

---

## 3단계. 디자인
PRD 3.5, 3.6을 적용한다.

- [ ] 가운데 카드 레이아웃을 만든다. 최대 폭 480px, 여백과 둥근 모서리, 부드러운 배경색.
- [ ] 글자 크기: 기본 20px 이상, 메시지 약 36px, 입력 숫자 약 32px. 버튼도 크게 만든다.
- [ ] 메시지 종류별 색을 준다: up / down / 정답(초록) / 실패(빨강) / 오류(회색). JS에서 클래스로 바꾼다.
- [ ] 남은 기회가 2번 이하이면 빨간색으로 강조한다.
- [ ] 잠긴 입력과 버튼은 흐리게 보이게 한다.
- [ ] 축하 효과: 꽃가루 div 여러 개를 만들어 CSS 애니메이션으로 약 3초간 떨어뜨리고 제거한다. 메시지에는 커졌다 돌아오는 효과를 준다.
- [ ] [다시 시작]을 누르면 남은 꽃가루를 즉시 제거한다.
- [ ] 모바일(360px 폭)에서도 깨지지 않게 한다.

**확인:** 정답을 맞혀서 효과를 보고, 브라우저 폭을 좁혀서 레이아웃을 확인한다.

---

## 4단계. 점검
PRD 6장 완료 기준으로 최종 확인한다.

- [ ] 인터넷을 끊은 상태에서 더블클릭으로 연다.
- [ ] 시나리오 테스트
  - [ ] 1~3번 안에 맞히기 → 성공 메시지, 꽃가루, 입력 잠김
  - [ ] 5번째 시도부터 짝홀 힌트가 보이고 정답과 일치한다.
  - [ ] 7번째에 맞히기 → 성공
  - [ ] 7번 모두 틀리기 → `실패! 정답은 N`, 입력 잠김, 남은 기회 0번
  - [ ] 빈 칸, 0, 101, 3.5, 같은 수 다시 입력 → 횟수가 줄지 않는다.
  - [ ] Enter 키로 제출된다.
  - [ ] 게임 도중, 성공 후, 실패 후 각각 [다시 시작] → 완전히 초기화된다.
  - [ ] 꽃가루가 떨어지는 중에 [다시 시작] → 효과가 바로 사라진다.
- [ ] 콘솔 오류가 없는지, 디버그 출력이 남아 있지 않은지 확인한다.
- [ ] PRD 6장 체크리스트를 모두 체크한다.
