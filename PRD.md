# PRD: 업다운 숫자 맞히기 게임

## 1. 개요
쉬는 시간에 혼자 가볍게 즐기는 숫자 게임이다.
컴퓨터가 1~100 사이의 숫자를 정하면, 사용자가 업/다운 힌트를 보며 범위를 좁혀 **7번 안에** 정답을 맞힌다.

- **목표:** 힌트로 범위를 좁혀 가다 제한 안에 맞히는 재미를 준다.
- **사용자:** 혼자 짧게(1~2분) 플레이하는 사람
- **형태:** `index.html` 파일 하나. 더블클릭하면 브라우저에서 바로 열린다.

## 2. 게임 규칙 (요구사항)

| # | 요구사항 |
|---|---|
| R1 | 게임을 시작하면 컴퓨터가 1~100 사이 정수 하나를 무작위로 정한다. |
| R2 | 숫자를 입력하고 [확인]을 누르면 결과를 보여 준다.<br>· 입력 > 정답: **더 작은 수를 입력하세요**<br>· 입력 < 정답: **더 큰 수를 입력하세요**<br>· 입력 = 정답: **정답입니다!** |
| R3 | 시도 횟수를 항상 보여 준다. (예: `시도 횟수: 2번`) |
| R4 | 기회는 7번이다. 남은 기회를 항상 보여 준다. (예: `남은 기회: 3번`) |
| R5 | 7번을 모두 틀리면 **실패! 정답은 42** 처럼 정답을 보여 준다. |
| R6 | 맞히거나 실패하면 입력창과 [확인] 버튼을 잠근다. |
| R7 | 4번 틀린 뒤부터 입력창 아래에 **짝수예요** 또는 **홀수예요** 힌트를 보여 준다. |
| R8 | [다시 시작]을 누르면 새 숫자를 정하고 모든 상태를 처음으로 되돌린다. |
| R9 | 글씨는 크게 보여 준다. |
| R10 | 정답을 맞히면 축하 효과(꽃가루)를 보여 준다. |

## 3. 직접 정한 사항 (요청에 없던 부분)

### 3.1 입력 처리
- **Enter 키**로도 [확인]과 똑같이 제출된다.
- 제출 후 입력창을 비우고 다시 입력창에 커서를 둔다. 바로 다음 숫자를 칠 수 있다.
- **잘못된 입력은 시도로 세지 않는다.** 결과 메시지 자리에 안내만 보여 준다.
  - 빈 칸: `숫자를 입력하세요`
  - 정수가 아님 / 1~100 밖: `1부터 100 사이의 정수를 입력하세요`
- **이미 입력한 숫자도 시도로 세지 않는다.** 안내: `이미 입력한 숫자예요 (57)`
- 입력창은 `type="number"`, `min=1`, `max=100`을 쓴다. 검사는 스크립트에서 다시 한다.

### 3.2 횟수와 힌트 기준
- 시도 횟수는 **유효한 입력**만 센다. 남은 기회 = 7 − 시도 횟수.
- 7번째 시도에서 맞히면 성공이다. (실패보다 성공 판정이 먼저다.)
- 짝홀 힌트는 **틀린 횟수가 4 이상이고 게임이 진행 중일 때** 보인다. 즉 5번째 시도부터 보인다.
- 게임이 끝나면 짝홀 힌트는 숨긴다. 결과 메시지에 집중하게 한다.

### 3.3 화면 상태
| 상태 | 결과 메시지 | 입력/확인 | 짝홀 힌트 |
|---|---|---|---|
| 시작 | `1부터 100 사이의 숫자를 맞혀 보세요!` | 사용 가능 | 숨김 |
| 진행 중 (틀린 수 1~3) | 업/다운 메시지 | 사용 가능 | 숨김 |
| 진행 중 (틀린 수 4~6) | 업/다운 메시지 | 사용 가능 | 보임 |
| 성공 | `정답입니다!` / `(N번 만에 맞혔어요)` (두 줄) | 잠김 | 숨김 |
| 실패 | `실패! 정답은 42` | 잠김 | 숨김 |

- [다시 시작] 버튼은 **언제나** 누를 수 있다.
- 결과 메시지 색: 더 큰 수는 초록 ▲, 더 작은 수는 빨강 ▼, 정답은 노랑, 실패는 흰색, 입력 오류는 회색.
  - 초록/빨강은 업/다운 전용으로 쓴다. 그래서 실패는 빨강이 아닌 흰색으로 둔다. "더 작은 수"와 헷갈리지 않게 하기 위해서다.

### 3.4 화면 구성 (위에서 아래 순서)
1. 제목: `업다운 숫자 게임`
2. 안내 한 줄: `1 ~ 100 사이, 기회는 7번`
3. 결과 메시지 (가장 크게)
4. 입력창 + [확인] 버튼
5. 짝홀 힌트 (입력창 바로 아래)
6. `시도 횟수: N번` · `남은 기회: N번`
7. [다시 시작] 버튼

### 3.5 디자인
- 디자인 기준: `DESIGN-binance.md`의 색과 모양 규칙을 따른다. 브랜드 이름과 로고는 쓰지 않는다.
  - 거의 검은 배경(#0b0e11) 위에 어두운 회색 카드(#1e2329) 하나. 그림자와 그라데이션 없이 색 차이로만 구분한다.
  - 강조색은 노랑(#fcd535) 하나만 쓴다. [확인] 버튼, 정답 메시지, 힌트 표시, 남은 기회 경고에만 쓴다.
  - 업/다운은 초록(#0ecb81) / 빨강(#f6465d)으로 표시한다.
  - [확인]은 노란 바탕에 검은 글씨다. 잠기면 어두운 노랑(#3a3a1f)으로 바뀐다. [다시 시작]은 한 단계 밝은 회색(#2b3139)이다.
- 기본 글자 크기 20px 이상. 결과 메시지는 약 36px, 입력창 숫자는 약 32px. 디자인 기준의 14px보다 이 크기를 우선한다.
- 화면 가운데에 카드 하나로 배치한다. 폭은 최대 480px이고, 휴대폰 폭(360px)에서도 깨지지 않는다.
- 남은 기회가 2번 이하이면 남은 기회 글자를 노란색으로 강조한다.
- 글꼴은 시스템 기본 글꼴(Segoe UI, 맑은 고딕 등)을 쓴다. 외부 폰트는 쓰지 않는다. 디자인 기준의 전용 글꼴도 쓰지 않는다.

### 3.6 축하 효과
- 정답을 맞히면 꽃가루 조각(노랑·금색·초록·흰색)이 화면 위에서 약 3초 동안 떨어진다.
- 동시에 결과 메시지가 살짝 커졌다 돌아오는 애니메이션을 준다.
- **외부 라이브러리 없이** HTML/CSS/JavaScript만으로 만든다. 인터넷 없이도 동작해야 한다.
- 효과가 끝나면 꽃가루 요소를 지운다. [다시 시작]을 누르면 진행 중인 효과도 바로 지운다.

## 4. 기술 조건
- 파일은 `index.html` 하나다. CSS와 JS를 파일 안에 넣는다.
- 서버와 빌드가 없고 외부 CDN도 쓰지 않는다. 더블클릭(`file://`)으로 열려야 한다.
- 대상 브라우저: 최신 Chrome / Edge.
- 정답 생성: `Math.floor(Math.random() * 100) + 1`
- 저장 기능(기록, 최고 점수)은 없다. 새로고침하면 새 게임이 된다.

## 5. 하지 않는 것 (범위 밖)
- 난이도 선택, 범위 변경
- 최고 기록 저장, 순위표
- 소리 효과
- 여러 명이 함께하는 모드

## 6. 완료 기준
- [x] 더블클릭하면 인터넷 없이 게임이 열린다.
- [x] R1~R10을 모두 만족한다.
- [x] 잘못된 입력과 중복 입력이 시도 횟수를 줄이지 않는다.
- [x] 7번째에 맞히면 성공, 7번 모두 틀리면 실패가 뜬다.
- [x] 5번째 시도부터 짝홀 힌트가 맞게 보인다.
- [x] 끝난 뒤 입력이 잠기고, [다시 시작]으로 완전히 초기화된다.
- [x] 360px 폭 화면에서도 레이아웃이 깨지지 않는다.
